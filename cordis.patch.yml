# dsh-enhanced-model-selector bundle patch.
# Pure client plugin — no host row needed. The browser half auto-registers
# via the package.json dsh.client declaration (client registry serves lib/client.js).
# This file exists so dsh.bundle.patch is declared, making DSH recognize
# this package as a profile layer (not just a plain dependency).
[]
